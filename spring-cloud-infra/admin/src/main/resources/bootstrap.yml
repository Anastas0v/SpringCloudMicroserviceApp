spring:
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    enabled: true